<h1>Get this out of index?</h1>

<ul>
  <% @products.each do |product| %>
    <li>
    <%= link_to product.title, product_path(product) %> <br/>
      <small>
        Posted <%= time_ago_in_words(product.created_at) %> ago
      </small>
    </li>
  <% end %>
</ul>